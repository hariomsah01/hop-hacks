/**
 * Analysis tab canvas. Intentionally empty so the analytics UI can be built
 * here without touching the map view.
 */
export default function AnalyticsView() {
  return <div className="h-full bg-[var(--color-canvas)]" />;
}
