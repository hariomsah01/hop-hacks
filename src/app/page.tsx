import PantryTwinApp from "@/components/PantryTwinApp";

/**
 * The map dashboard is the landing route on purpose: the guide calls for
 * immediate access to the map rather than a splash page.
 */
export default function Page() {
  return <PantryTwinApp />;
}
